import 'package:flutter/material.dart';

const landingTitleShadow = Shadow(color: Colors.black, blurRadius: 20.0);
const landingTitleStyle = TextStyle(
    color: Colors.white, fontSize: 100.0, shadows: [landingTitleShadow]);
const landingSubtitleShadow = Shadow(color: Colors.black, blurRadius: 6.0);
const landingSubtitleStyle = TextStyle(
    color: Colors.white, fontSize: 30.0, shadows: [landingSubtitleShadow]);
const landingBodyShadow = Shadow(color: Colors.black, blurRadius: 3.0);
const landingBodyStyle = TextStyle(
    color: Colors.white, fontSize: 16.0, shadows: [landingBodyShadow]);
const landingBoldBodyStyle = TextStyle(
    color: Colors.white,
    fontSize: 16.0,
    shadows: [landingBodyShadow],
    fontWeight: FontWeight.bold);
